"""
processes — thin process entry points (lifecycle and loop logic only).

    processes/orchestrator.py  — scheduling, single-instance lock, signal handling
    processes/monitor.py       — polling interval, jitter, SIGHUP wiring
"""
